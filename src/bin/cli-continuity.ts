// Placeholder scaffold for DC-LEDGER.
// The implementation lead pre-creates this file so the projection
// system admits `src/bin/cli-continuity.ts` for the
// continuity-ledger-worker child worktree. The child overwrites the
// placeholder with the read-only `conduct continuity-report`
// command (spec §12) using the production log reader and the same
// pure materializer/renderer pair.
export {};
