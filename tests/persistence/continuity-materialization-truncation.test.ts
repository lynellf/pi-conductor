// Placeholder scaffold for DC-LEDGER.
// The implementation lead pre-creates this file so the projection
// system admits `tests/persistence/continuity-materialization-truncation.test.ts`
// for the continuity-ledger-worker child worktree. The child
// overwrites the placeholder with focused tests covering the
// deterministic bounded seed selection, atomic truncation, and
// omission counts (spec §11, §16).
export {};
