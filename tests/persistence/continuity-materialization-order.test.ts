// Placeholder scaffold for DC-LEDGER.
// The implementation lead pre-creates this file so the projection
// system admits `tests/persistence/continuity-materialization-order.test.ts`
// for the continuity-ledger-worker child worktree. The child
// overwrites the placeholder with focused tests covering canonical
// record order, active/superseded state, byte-identical replay,
// and supersession reference rejection (spec §16).
export {};
